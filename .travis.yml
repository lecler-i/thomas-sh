language: node_js

branches:
  only:
  - master

script:
- yarn build

deploy:
  provider: pages
  skip_cleanup: true
  repo: 'lecler-i.github.io'
  github_token: $GITHUB_TOKEN
  on:
    branch: master
  local_dir: 'public'
